# Example environment variables for Spotify auth
# Copy this to `.env.local` and fill in the values (do NOT commit `.env.local` with secrets)

VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
# VITE_SPOTIFY_REDIRECT_URI should match the redirect URI you set in your Spotify app settings
# For local development this is often something like: http://localhost:5173/spotify-callback
VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/callback
# Space-separated scopes to request. Adjust to your needs.
VITE_SPOTIFY_SCOPES="user-read-private user-read-email user-top-read playlist-modify-private"
